#ifndef _baseData_
#define _baseData_
#include <math.h>
#define PI acos(-1)

const int DATA_NUM = 1080; //雷达数据个数
const float R = 0.15 ; // 柱半径
const float ANGLE_INCREMENT = PI/1080;  // 雷达角度增量(3 * PI / 2) / 360
#endif

